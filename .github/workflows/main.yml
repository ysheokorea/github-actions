name: GithubActions Test

on: 
  push: 
    branches : 
    - main
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: checkRoot
      run: pwd
    - name: ls -al
      run : ls -al
    - name: check disk
      run : df -h
    - name : Node execution
      run : node index.js
    
        
    
